<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="product">

	<insert id="addproducts" parameterType="java.util.Map">
	<![CDATA[
	insert into product(name, price, image, summary_desc, detail_desc, manufacturing_date, display,
	material, provider, manufacturer, origin) values
	]]>
	<foreach collection="productList" item="item" separator=" , ">
	<![CDATA[
            (#{item.name}, #{item.price},'imageurl', '요약설명', 
			'상세설명', '2019-07-15', true, '원재료', '제공업체', '제조업체', '원산지')
    ]]>
        </foreach>
	</insert>
	
	<select id="selectByEmail" parameterType="string" resultType="membervo">
	<![CDATA[
	select * from member where email=#{email };
	]]>
	</select>
	
	<select id="selectByEmailandPassword" parameterType="membervo" resultType="membervo">
	<![CDATA[
	select * from member where email=#{email } and password=#{password };
	]]>
	</select>
	
	<delete id="deleteByEmail" parameterType="string">
	<![CDATA[
	delete from member where email=#{email };
	]]>
	</delete>
	
</mapper>
